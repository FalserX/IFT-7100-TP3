import { ProductType } from "@/types/product";
import { v4 as uuid } from "uuid";

const ProductForm = ({
  id,
  cost,
  name,
  qty,
  merchant,
  imageSrc,
  imageAlt,
}: ProductType) => {
  return <></>;
};

export default ProductForm;
